name: 'Sample Subprofile'
type: profile
version: 8.x-dev
core: '8.x'

themes:
  - bartik
  - seven

dependencies:
  - default_content

config_install: true

base profile:
  name: lightning
  excluded_dependencies: {}
